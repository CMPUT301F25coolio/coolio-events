package com.example.coolioevents.organizer;
import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
/*
  Placeholder screen for now
  I kept this file around because the backlog might later
  split MyEvents into separate pages like history or stats.
  Right now it just loads a blank screen.*/
public class MyEventsy extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // not setting any layout yet this one is basically empty for milestone 3
    }
}
